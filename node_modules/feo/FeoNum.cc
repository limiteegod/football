#include "FeoNum.h"

FeoNum::FeoNum(char *pNum, int len)
{
    this->numLen = 0;
    this->pNumArray = (long *)malloc(sizeof(void *)*5);

    /*char *pOneNum = (char *)malloc(sizeof(char)*30);
    int len = 0;
    char *pTmp = pNum;
    for(int i = 0; i < len; i++)
    {
        char c = pTmp[0];
        if(c == ';')    //单式各注号码之间使用,进行分隔
        {

        }
        else
        {

        }
        pTmp++;
    }*/
}

FeoNum::~FeoNum()
{
    long *pTmp = this->pNumArray;
    for(int i = 0; i < this->numLen; i++)
    {
        char *pNum = (char *)pTmp[i];
        delete[] pNum;
    }
    delete[] this->pNumArray;
}