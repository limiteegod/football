#include "FeoNumUtil.h"

FeoNumUtil::FeoNumUtil()
{
}

FeoNumUtil::~FeoNumUtil()
{
}

char * FeoNumUtil::GetCharArray0000(char *pNum, int len)
{
    for(int i = 0; i < len; i++)
    {
        char c = pNum[0];
        if(c == ';')
        {

        }
        pNum++;
    }
}